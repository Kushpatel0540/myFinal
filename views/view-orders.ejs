<!-- Name: Kush Vishnubhai Patel
Number: 8846040
Email: Kpatel6040@conestogac.on.ca
Date: 17 August, 2023 -->

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>View Orders</title>
 
</head>
<body>
  <h1>View Orders</h1>
  <table>
    <tr>
      <th>Name</th>
      <th>Student ID</th>
      <th>HTML5 Books</th>
      <th>CSS3 Books</th>
      <th>Pen</th>
      <th>Subtotal</th>
      <th>Tax</th>
      <th>Total</th>
    </tr>
    <% orders.forEach(order => { %>
      <tr>
        <td><%= order.name %></td>
        <td><%= order.studentId %></td>
        <td>
          <ul>
            <% order.items.forEach(item => { %>
              <li><%= item %></li>
            <% }) %>
          </ul>
        </td>
        <td>$<%= order.subTotal.toFixed(2) %></td>
        <td>$<%= order.tax.toFixed(2) %></td>
        <td>$<%= order.total.toFixed(2) %></td>
      </tr>
    <% }) %>
  </table>
</body>

<br><br>
<footer>
    &copy; Copyright - Kush Vishnubhai Patel
  </footer>
</html>
